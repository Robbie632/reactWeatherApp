const config = {
  sensorServerURL: "http://192.168.1.143:8000/sensor", // url of server outputting sensor info
  fetchSensorInterval: 1000 // ms, interval between get to server
};

export default config;